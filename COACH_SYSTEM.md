# 🎯 ระบบ Coach Management - คู่มือการใช้งาน

## ✅ สร้างสำเร็จแล้ว!

### 📁 ไฟล์ใหม่ที่สร้าง:
1. **coach-login.html** (5.8 KB) - หน้าเข้าสู่ระบบ Admin
2. **coach-dashboard.html** (17.3 KB) - หน้าจัดการโค้ช
3. **backend.js** (อัปเดต) - เพิ่มฟังก์ชันโค้ช
4. **index.html** (อัปเดต) - ดึงข้อมูลโค้ชมาแสดง

---

## 🔐 การเข้าสู่ระบบ Coach Admin

### ขั้นตอนที่ 1: เข้าสู่ระบบ
- เปิด: `http://localhost:3000/coach-login.html`
- **Username:** `admin`
- **Password:** `admin123`

### ขั้นตอนที่ 2: จัดการโค้ช
หลังจากเข้าสู่ระบบสำเร็จ จะเข้าสู่หน้า Dashboard

---

## 🎨 ฟีเจอร์ Coach Dashboard

### 1. เพิ่มโค้ชใหม่
- กดปุ่ม "เพิ่มโค้ชใหม่"
- กรอกข้อมูล:
  - **รูปโค้ช:** 512x512 px | สูงสุด 2 MB
  - **ชื่อโค้ช:** เช่น โค้ชแอน
  - **รหัสโค้ช:** 6 หลัก (เช่น SKM123)
  - **ตำแหน่ง:** เช่น SKM.WELLNESS COACH
  - **คำพูดประจำตัว:** ข้อความแนะนำตัว
  - **LINE ID:** เช่น @coach.ann
  - **เบอร์โทร:** เช่น 02-xxx-xxxx
- กดบันทึก

### 2. แก้ไขข้อมูลโค้ช
- กดปุ่ม "แก้ไข" ที่การ์ดโค้ช
- แก้ไขข้อมูล
- กดบันทึก

### 3. ลบโค้ช
- กดปุ่ม "ลบ" ที่การ์ดโค้ช
- ยืนยันการลบ

---

## 🔗 การเชื่อมต่อกับหน้าหลัก

### หน้าที่แสดงข้อมูลโค้ช:

#### **หน้า 5: Mini Challenge**
- รูปโค้ช + ชื่อโค้ช

#### **หน้า 16: ข้อมูลโค้ช**
- รูปโค้ช (ขนาดใหญ่)
- ชื่อโค้ช
- ตำแหน่ง
- คำพูดประจำตัว
- ปุ่ม "เพิ่มเพื่อน LINE" (คลิกแล้วเปิด LINE)
- ปุ่ม "โทรหาผู้ดูแล" (คลิกแล้วโทรออก)

#### **Bottom Navigation**
- รูปโค้ช (ที่ปุ่มเมนู 4)

---

## 💾 ระบบจัดเก็บข้อมูล

### LocalStorage:
- `skm_coaches` - รายชื่อโค้ชทั้งหมด
- `selected_coach` - โค้ชที่เลือก (จากรหัสโค้ช)
- `skm_coach_code` - รหัสโค้ชที่กรอก

### โครงสร้างข้อมูล Coach:
```json
{
  "id": "1",
  "name": "โค้ชแอน",
  "code": "SKM123",
  "title": "SKM.WELLNESS COACH",
  "quote": "ดูแลคุณเหมือนคนในครอบครัว...",
  "photo": "data:image/jpeg;base64,...",
  "line": "@coach.ann",
  "phone": "02-xxx-xxxx",
  "createdAt": "2025-09-17T12:00:00.000Z"
}
```

---

## 🔄 Flow การทำงาน

### 1. Admin เพิ่มโค้ช
```
Coach Login → Dashboard → เพิ่มโค้ชใหม่ → อัปโหลดรูป + กรอกข้อมูล → บันทึก
```

### 2. ผู้ใช้กรอกรหัสโค้ช
```
หน้า 15 → กรอกรหัสโค้ช (เช่น SKM123) → ยืนยัน → เลือกโค้ช → แสดงที่หน้า 16
```

### 3. ระบบโหลดข้อมูลโค้ช
```
DOMContentLoaded → loadCoachInfo() → ดึงข้อมูลจาก backend.getSelectedCoach() → อัปเดต DOM
```

---

## 🎯 การใช้งานจริง

### ตัวอย่าง: เพิ่มโค้ชใหม่

1. เข้า `http://localhost:3000/coach-login.html`
2. Login: admin / admin123
3. กด "เพิ่มโค้ชใหม่"
4. อัปโหลดรูป (512x512 px)
5. กรอก:
   - ชื่อ: โค้ชอ้อม
   - รหัส: SKM999
   - ตำแหน่ง: Senior Health Coach
   - คำพูด: "มาลดน้ำหนักด้วยกันนะคะ"
   - LINE: @coach.om
   - โทร: 02-123-4567
6. กดบันทึก

### ทดสอบในหน้าหลัก:

1. กลับไป `http://localhost:3000`
2. ไปที่หน้า 15 (ใส่รหัสโค้ช)
3. กรอก: SKM999
4. กดยืนยัน
5. ไปหน้า 16 → จะเห็นข้อมูลโค้ชอ้อมที่เพิ่มไว้
6. ตรวจสอบหน้า 5 และ Bottom Nav → รูปโค้ชอัปเดตแล้ว

---

## 🔒 ความปลอดภัย

### Session-based Authentication:
- ใช้ `sessionStorage` เก็บ session
- ต้อง login ใหม่ทุกครั้งที่ปิดแท็บ
- ป้องกันการเข้าถึง Dashboard โดยตรง

### การเปลี่ยนรหัสผ่าน:
แก้ไขใน `coach-login.html` บรรทัด 88-91:

```javascript
const ADMIN_CREDENTIALS = {
  username: 'admin',
  password: 'รหัสผ่านใหม่' // เปลี่ยนตรงนี้
};
```

---

## 🐛 Troubleshooting

### ปัญหา: รูปโค้ชไม่แสดง
- ตรวจสอบขนาดไฟล์ (ต้องไม่เกิน 2 MB)
- ตรวจสอบ Browser Console (F12)
- รีเฟรชหน้า

### ปัญหา: Login ไม่ได้
- ตรวจสอบ username และ password (case-sensitive)
- ล้าง sessionStorage: `sessionStorage.clear()`

### ปัญหา: ข้อมูลโค้ชไม่อัปเดต
- ตรวจสอบ LocalStorage: F12 → Application → Local Storage
- ลองรีเฟรชหน้า (Ctrl+R)

---

## 📞 เข้าถึงหน้า Coach Admin

### วิธีที่ 1: URL โดยตรง
```
http://localhost:3000/coach-login.html
```

### วิธีที่ 2: จากหน้า Settings
```
หน้าหลัก → กดปุ่มเฟือง (⚙️) → เลื่อนลงล่าง → คลิก "Coach Admin"
```

---

## ✨ สรุป

ระบบ Coach Management ที่สร้างมี:
- ✅ หน้า Login มีรหัสผ่านป้องกัน
- ✅ Dashboard จัดการโค้ช (เพิ่ม/แก้ไข/ลบ)
- ✅ อัปโหลดรูปโค้ช (เก็บเป็น Base64)
- ✅ ดึงข้อมูลโค้ชแสดงหน้า 5, 16, Bottom Nav
- ✅ ระบบตรวจสอบรหัสโค้ช (หน้า 15)
- ✅ เชื่อมต่อ LINE และโทรศัพท์

**พร้อมใช้งาน 100%!** 🎉
